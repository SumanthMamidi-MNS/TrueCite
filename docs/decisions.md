# Decisions log

- 2026-09-12: Use `pypdf` for PDF parsing in Phase 1 instead of `unstructured`, because `unstructured`'s system dependencies (poppler/tesseract) are heavy to set up on Windows and pypdf is sufficient for text-based legal PDFs; revisit if scanned/image-only PDFs show up in the corpus.
- 2026-09-12: Local git repo initialized, no GitHub remote yet — commits stay local until the user explicitly asks to push/create a repo.
- 2026-09-12: Python dependency management via venv + requirements.txt (user preference).
- 2026-09-12: Structural chunker uses a cascading strategy (numbered sections/paragraphs -> subsection split if oversized -> paragraph fallback if no numbering detected) instead of one fixed pattern, because the corpus's three document types (Act, numbered-paragraph guideline, plain FAQ) each use different structure.
- 2026-09-12: Section-number detection runs once globally over a document's full body text (not per-chapter), because Act section numbers are continuous across chapters — per-chapter matching let page-footnote numbering (which restarts low) collide with real section numbers again at each new chapter.
- 2026-09-12: Square-bracket amendment-insertion markers (e.g. "3[11A. ...") are stripped from page text before chunking — they're pure editorial typography in Indian bare acts, and left in place they hide the real section number from line-start detection (was silently merging section 11A's text into section 11).
- 2026-09-12 (superseded, see below): Lettered definition clauses were initially left un-split.
- 2026-09-12: `chunk_document`'s `body_start_anchor` now raises if the anchor string isn't found, instead of silently chunking the whole document (TOC included) as body text. Caught a real bug this way (PDF extraction's stray spaces/line-wraps broke both new anchors) instead of shipping bad output that looked plausible.
- 2026-09-12: Oversized sections with no numeric "(1)"/"(2)" subsections (AYUSH-2025's decimal "3.1" headings, WIPO toolkit's named headings) now fall back to paragraph-grouping rather than a second dedicated parser for decimal/named headings — reuses the same fallback machinery as unstructured documents instead of adding a new strategy, at the cost of citing by paragraph position instead of a specific numbered subsection for these two docs.
- 2026-09-12: Paragraph splitting/merging reworked to operate on character spans (start,end) instead of extracted strings re-searched in the original text — the string-search approach crashed on real input because reconstructed "\n\n"-joined text didn't always match the source's original whitespace verbatim.
- 2026-09-12: Vector store for Phase 2 is ChromaDB (embedded library, matches PRD §7), not Qdrant, even though Qdrant is already running in the user's Docker Desktop. Reason: at this corpus size (288 chunks) performance is a non-issue either way, and ChromaDB has no separate server to start/forget-to-start, which removes friction during active retrieval-logic iteration. Qdrant's real advantages (native hybrid search, richer filtering, built for concurrent scale) matter for production multi-user deployment, which the PRD's non-goals explicitly rule out for this project.
- 2026-09-12: Lettered clauses ("(a)", "(b)", ... "(p)") are now split into their own chunks whenever a section enumerates >=3 of them and has no numeric subsections of its own — regardless of total section length. Motivated by a concrete finding while hand-checking Phase 2's gate queries: Section 3 (16 unrelated statutory exclusions in one chunk) didn't surface for a plain-language TK-patentability question even in the top 20 vector results, because pooling all 16 clauses' embeddings diluted clause (p)'s specific signal (isolating it alone raised cosine similarity 0.455 -> 0.568). Each clause chunk keeps the section's intro text prepended so it isn't read out of context.
- 2026-09-12: Lettered-clause matches are filtered to a strictly-increasing sequence (same defense as the existing section-number monotonic filter), because a lettered marker is short enough that a mid-sentence cross-reference (e.g. "...described in clause (b) of this section...") can start its own PDF-wrapped line and be mistaken for a second, spurious "(b)".
- 2026-09-12: Fixed a second, independently-discovered instance of the "content silently dropped before the first regex match" bug class: `_split_section_body`'s numeric-subsection splitting was dropping subsection "(1)"'s entire text whenever it appeared inline with the section heading (the normal case: "19. Powers...—(1) If...") rather than on its own line. Also had to loosen the function's "need >=2 matches to bother splitting" gate to count this inline "(1)" toward the total, since a section with exactly two subsections ("(1)" inline + "(2)" on its own line) was hitting the gate before ever reaching the fix.
- 2026-09-12: Added a pytest suite (tests/, 22 tests) covering chunking, BM25, RRF fusion, and authority metadata — formalizes what had been ad-hoc verification via inline scripts into a real regression suite that would catch these bugs automatically going forward.
- 2026-09-12: After the clause-splitting fix, Section 3(p) still doesn't rank in the top 20-30 vector results (or BM25, or hybrid) for "Can traditional knowledge be patented in India?" — verified this is a vocabulary/framing gap (the statute never uses the word "patent"; the query does), not a remaining chunking defect, and did not chase it further with more chunking engineering since that would be fixing the wrong layer. Accepted as a known challenge to carry into Phase 5 (Layer 3 should be able to promote an Act-level citation over a same-topic Guideline citation, since similarity ranking alone won't reliably surface terse statutory clauses for natural-language questions) rather than something Phase 2/3 chunking or retrieval tuning can fully solve.
- 2026-09-12: Layer 1 confidence-gate threshold set at 0.90 (ChromaDB L2-on-normalized-embeddings distance), calibrated against real (query, distance) evidence rather than guessed: every genuinely on-topic query tested had best-hit distance <= ~0.63; every genuinely unrelated query (cricket rules, a cake recipe, boiling point, LLC tax) scored >= ~0.99. A real middle zone exists (0.76-0.82) for queries the corpus is topically related to but doesn't actually answer with specifics (e.g. asking for a fee *amount* when the Act only says fees are "as may be prescribed") — the threshold deliberately lets these through rather than refusing, since Layer 2 (claim-support verification) is the layer designed to catch "relevant-looking but not actually responsive," not Layer 1. First calibration from ~20 queries; revisit with the full Phase 6 eval set.
- 2026-09-12: Layer 2 (claim-support verification) uses a local Ollama model (Qwen 2.5 7B, chosen over the also-available Gemma 2 2B and Llama 3.1 8B for its stronger instruction-following/reasoning at this scale — matters because the whole layer is a precision judgment call) instead of the Claude API the PRD specifies for this layer. Reason: no Anthropic API key is configured for this project yet. Explicit, temporary substitution the user chose after I flagged the blocker, not a silent architecture change — revisit once a key exists. Verified live against 3 real cases: correctly supports a clear TK-non-patentability claim, correctly rejects a claimed specific fee amount against a passage that only says fees are "as prescribed" (the exact case Layer 1's distance threshold alone could not catch — 0.82 distance, under the 0.90 gate), and correctly rejects a claim that actively contradicts a passage about Biological Diversity Act approval requirements. `verify_claim` raises rather than defaults to a verdict on any malformed/incomplete model response, since a verification layer that fails open on a parse error defeats its own purpose.
- 2026-09-12: Layer 2 upgraded to majority-vote verification (3 calls, take the consensus), after directly observing the local 7B model give inconsistent verdicts on the SAME (claim, passage) pair across repeated calls — 2-of-3 correctly supported a genuinely well-supported claim (the PIB patent-count statistic), 1-of-3 incorrectly rejected it (failed to resolve "these" back to its antecedent in the prior sentence). A single unlucky call would have silently produced a false refusal on a correct answer. Also reordered the verification JSON schema to put "reasoning" before "supported" (reason-then-decide, not decide-then-rationalize) — this alone roughly halved the error rate on the same test case before majority voting was added on top. Both changes are direct responses to observed evidence, not speculative hardening.
- 2026-09-12: End-to-end pipeline (`src/generate.py`) initially had a real bug caught by testing on the exact PIB-statistic query used throughout Phase 2-4: authority ordering was applied to the WHOLE candidate pool before truncating to top_k, so several lower-relevance, higher-authority-tier chunks (AYUSH-2025 boilerplate) pushed the actually-relevant chunk (the PIB press release's specific statistic) out of the top_k entirely, causing a false refusal on a query the system had already proven it could answer well in Phase 2/3. Fixed: truncate to top_k by relevance (hybrid rank) FIRST, then apply authority ordering only within that already-selected set — authority is a citation-priority/presentation preference among sources already established as relevant, not a relevance signal itself. Covered by `tests/test_generate.py`.
- 2026-09-12: Generation prompt instructs the model to return an empty claim list rather than state ungrounded specifics, and this worked in practice without Layer 2 needing to intervene: asked for the current patent filing fee (corpus only says fees are "as prescribed," no figures), generation correctly returned zero claims rather than fabricating a number. Layer 2's rejection of a fabricated fee claim was separately verified by constructing that claim directly (see the entry above) — both safety nets are real, not just one covering for an assumption about the other.
- 2026-09-12: `generate.py` switched from raw chunk_id strings to numbered passage labels ([1], [2], ...) for generation to cite, after directly observing the model paraphrase/truncate a chunk_id (e.g. return "ipo_ayush_examination_guidelines_2025" instead of "ipo_ayush_examination_guidelines_2025::preamble") when asked to copy it — the exact-string lookup then silently failed and dropped an otherwise-correct, well-grounded claim, producing a false refusal on a question already validated as answerable in Phase 2. A bare integer is far less prone to that kind of copy error. Found via Phase 6 eval testing, not assumed.
- 2026-09-12: `generate.py`'s CANDIDATE_K raised 20->40 after finding a genuinely correct source chunk (`patents_act_1970::sec-3-clause-d`) ranked #29 by vector similarity for its matching query — outside the previous fetch window entirely, so it was never even considered a candidate regardless of downstream logic. Same statutory-vocabulary-gap pattern documented for §3(p) in Phase 2 (a terse clause vs. a natural-language question), now confirmed recurring on a second, unrelated query — treated as a real, generalizable pattern rather than a one-off. This change is cheap (pure retrieval, no extra LLM calls).
- 2026-09-12: The CANDIDATE_K widening above caused a real regression on re-test (false-refusal rate rose from 5/11 to 7/11) — investigated rather than reverted blindly. Root cause: the generation prompt's "prefer the more authoritative source when they cover the same point" instruction was being over-applied — with more candidates in view, the model started picking a generic, higher-authority-tier passage over a more specific, lower-tier one that actually answered the question, because it satisfied "prefer authority" without checking "do they actually cover the same point" first. Fixed by rewriting the instruction to require finding the most specific/on-point passage FIRST, using authority order only as a tie-breaker among equally-specific candidates. This is a good example of why re-testing after a fix matters — the first fix looked complete until measured again.
- 2026-09-12: `run_phase6.py`'s eval ground truth corrected from one expected doc_id per question to a set of acceptable doc_ids, after discovering during investigation that a citation I'd scored as "wrong" was actually correct: `patents_act_1970::sec-10-sub-4` states the exact same biological-material-disclosure rule as the "expected" TK Guidelines chunk, directly from the Act itself (clause (ii)(D)) — I'd only read the first 600 characters of that passage back in Phase 1/2 and missed it. The single-expected-doc_id scoring was systematically undercounting correct answers whenever the system found a different, equally valid source. Every affected question's corrected ground truth was re-verified against actual chunk text, not guessed or loosened arbitrarily.
- 2026-09-12: Final Phase 6 false-refusal rate (5/11) traced to a genuine local-7B-model extraction-quality limitation, not a further pipeline bug — for the WIPO-toolkit "three phases" question, the exact right sentence was present in the #1-ranked candidate chunk, but generation extracted an unrelated illustrative example from later in that same long, multi-topic chunk instead. Accepted as a documented cost of the temporary local-LLM substitution (see the Layer 2 Qwen 2.5 7B entry above) rather than chased with further prompt engineering, given diminishing returns after 4 confirmed pipeline bugs already found and fixed via this same eval. A stronger model (i.e. the Claude API the PRD specifies) would very likely resolve it.
- 2026-09-12: Observed genuine run-to-run non-determinism in the full pipeline — identical code and questions produced different individual pass/fail patterns across repeated runs (aggregate false-refusal rate was stable at 5/11 across 3 of 4 runs, but which specific questions failed varied). Consistent with the Layer 2 majority-vote finding; a single eval run's numbers should be read as indicative of typical behavior, not an exact, reproducible score.
- 2026-09-13: Replaced the Streamlit UI with FastAPI + a hand-written HTML/CSS/JS front end in `web/`. Streamlit re-renders the whole page per interaction and gives no control over transitions, so it can't show the pipeline working live — and the pipeline working live is the product's whole differentiator, not decoration. The PRD (§7) already allows either. No build step or framework, so there's nothing between the source and a demo screen that can break.
- 2026-09-13: The UI is a chat application, not a search page. First version was a single-shot query form; rebuilt after user feedback pointed out the real usage pattern — an IP officer asks, reads, asks a follow-up, copies a citation into a file note, and comes back the next day wanting the thread. That dictates: sidebar of past consultations (localStorage), a scrolling thread, and a composer pinned to the bottom so results never render somewhere the user has to hunt for.
- 2026-09-13: `generate.answer_query_streaming` added as a generator yielding one event per pipeline stage, with `answer_query` reduced to a thin wrapper that drains it — no logic duplicated, and every existing test and gate script keeps working untouched. `/api/ask` forwards those events as SSE so the UI can show each layer's decision as it happens; a local model takes 20-40s, and that time is more honestly spent showing the verification than hiding it behind a spinner.
- 2026-09-13: The verification stepper collapses to a one-line badge once it settles, rather than staying expanded. Expanded-forever would make a daily-use thread unreadable after a few questions; collapsed-by-default would bury the one thing that distinguishes this system. Collapsing on completion (re-expandable) gets both.
- 2026-09-13: Follow-up questions are resolved by rewriting them into a standalone question (`generate._condense_followup`, one extra local-LLM call using the last 3 turns) BEFORE retrieval, rather than feeding conversation history into generation/verification directly. This keeps the grounding guarantee intact: a bad rewrite can only mis-target retrieval, which Layer 1/2 still catch (verified live — a genuinely ambiguous test follow-up produced a plausible-but-wrong rewrite, and the pipeline correctly refused rather than fabricating an answer). Feeding history straight into generation would let it assert something from the conversation instead of the corpus, defeating the whole point of Layer 2.
- 2026-09-13: `GENERATION_MODEL` (generate.py) and `MODEL` (verification.py) now both read `OLLAMA_MODEL` from the environment (default `qwen2.5:7b`) instead of being hardcoded in two places, and a new `/api/config` endpoint exposes the value so the UI's model badge always names whatever is actually configured — swapping models means setting one env var, not editing code and the UI to match.
- 2026-09-13: Decided against a table/column renderer for comparison-style questions. Every claim in this pipeline is verified against exactly one source passage (Layer 2's whole mechanism); a table cell comparing two things would need either fabricated structure the model was never asked to justify, or a second verification scheme for cells, which is a materially bigger change than this session's scope. The existing claim list — each claim citation-tagged with its source's authority tier — already lets a reader compare sources; flagged as a possible dedicated phase later rather than bolted on now.
- 2026-09-13: Second corpus-sourcing pass, at the user's request ("get as many documents and sources as possible"). Added two: the Biological Diversity Act, 2002 (closes a real gap found live during UI testing — its §55 penalty provision had only ever been indexed secondhand via the 2012 IPO guideline's summary) and the WIPO Treaty on IP, Genetic Resources and Associated Traditional Knowledge, 2024 (covers the "international regimes" half of the PRD's own problem statement, previously unrepresented). Rejected a third candidate — the Patents Rules, 2003 / Patents (Amendment) Rules, 2024 pair, which would also have given a genuine two-version authority-conflict test case — because the only available mirror of the base 2003 rules was a corrupted OCR scan (confirmed word-level corruption: "Govemment", "MINlSTRY", digit/letter confusion). The PRD's own Data Plan says a smaller clean corpus beats a large noisy one; that call was made literally, not just quoted. See `corpus/manifest.md` for the full sourcing trail including dead links tried.
- 2026-09-13: Extended `chunking.py` with a second structural-numbering pattern — treaty-style "ARTICLE N" with the title on the following line — activated only as a fallback tried after the existing numeric "N. Title.—" pattern finds fewer than 2 matches (see `_chunk_region`), so it's additive and can't change how any of the previously-verified 5 documents chunk. Reuses the existing oversized-section and lettered-clause splitting helpers rather than duplicating them. Chose this over a text-preprocessing hack (rewriting "ARTICLE N" into the numeric form before chunking) because that would have meant the stored chunk text no longer matched what the source document actually says — unacceptable for a citation-verification tool.
- 2026-09-13: `authority.py`'s new WIPO GRATK Treaty entry is deliberately tagged `Informational`, not `Act`, even though it's a real, adopted treaty text — because it is not yet in force (needs 15 ratifications/accessions, not yet reached as of sourcing). Its `short_name` says "not yet in force" directly in the string that gets shown in every citation built from it, so a generated answer can't accidentally present it as binding Indian law without that caveat riding along. Layer 3's authority ranking also means it will never be preferred over an actual Act or Guideline addressing the same point.
- 2026-09-13: The sidebar's "N primary sources indexed" line was hardcoded to "5" in `index.html`, gone stale the moment the corpus grew to 7 today. Fixed the same way as the model badge: `/api/config` now also returns `corpus_docs` (`len(authority.DOC_AUTHORITY)`), and the UI reads it — one source of truth, so the number can't go stale again next time the corpus changes.
- 2026-09-13: Live-verified the new corpus content post-rebuild and found a real, reproducible generation-quality issue: asking about the GRATK Treaty's Article 3 disclosure requirement — across 4 differently-worded attempts, including one naming "Article 3" directly — the local model kept drafting a claim describing "the Toolkit" (the pre-existing, separate WIPO TK documentation toolkit) even though the treaty's own Article 3 text was correctly retrieved and given to it every time (confirmed directly via `_select_grounded_hits_with_diagnostics`). Two WIPO-published documents both discussing documenting/disclosing genetic-resources-related TK appears to be a harder discrimination case for a 7B model than the corpus's other same-tier pairs. Added a source short_name label to each numbered passage in `_build_passages_block` as a partial mitigation (didn't fully fix it in testing, but is a strict improvement and harmless elsewhere). Critically, **Layer 2 correctly rejected every one of these mismatched claims** — the user never received a false answer, only a refusal — which is the system's core safety property holding under a new, harder case, not a new failure mode. Documented as an added-scope instance of the pre-existing local-model extraction-quality limitation (see Phase 6 findings), not a new class of bug. Not chased further today (would mean deeper prompt engineering against a model this project already plans to replace once an Anthropic API key exists).
- 2026-09-13: User has a real Anthropic API key but wants it held back until deployment, to avoid burning through its rate limits during development — reasonable, and not something to work around by using it anyway. Asked instead to keep the architecture "upgradable" so the swap is trivial when the time comes. Found this wasn't actually true: `generate.py`/`verification.py` called Ollama's HTTP API directly, with no Anthropic code path at all — "upgradable" was an assumption, not a fact. Fixed by extracting a single seam, `src/llm_client.py`, that both modules now call through; it defaults to the exact same Ollama behavior (verified live post-refactor: identical pipeline behavior, same citation output) and only calls the real API if `LLM_PROVIDER=anthropic` is explicitly set. The `anthropic` SDK is installed and the code path exists and is unit-tested (mocked), but is **not live-tested against a real key** by design — that first real call should happen at deployment, not before, and should be re-verified with the eval suite at that point rather than assumed to work identically to the mocked tests.
- 2026-09-13: Told the user directly that "zero limitations, nothing anyone can pinpoint" is the wrong target for a technical demo — judges probe for exactly this, and a refusal on a hard question is this project's core feature working, not a flaw to hide. Recommended owning the limitations list with a clear mitigation story instead of trying to erase it. Recorded here since it shaped what got built this round: the response was to make the *one legitimately fixable* structural gap (provider lock-in) actually fixable, not to chase the unfixable ones (local-model extraction quality) with more prompt-tuning, and not to remove or soften the documented limitations themselves.
- 2026-09-15: Corrected a real misunderstanding — the user asked if this could run on GitHub Pages. It can't: GitHub Pages is static-only (no Python, no server, no way to hold a secret key), and this project has a real FastAPI backend. Clarified the actual need is two separate things: GitHub as the *code repository* (already true, needed for "user downloads it"), and a genuinely Python-capable host for a live deployed instance (not yet chosen — Hugging Face Spaces or Render both fit this app's shape, Docker-friendly, free tier).
- 2026-09-15: Settled the run-mode split explicitly: Ollama stays the permanent, default, no-key mode for anyone who clones/downloads the repo and self-hosts — this was never in question, just needed stating clearly. For an actual hosted deployment, the user chose Gemini over the already-built Anthropic path (their reasoning: a more generous free tier makes it the safer default for a hackathon demo that might get real traffic). Added `LLM_PROVIDER=gemini` to `llm_client.py` as a second cloud path alongside (not replacing) Anthropic — both are real, reviewed, unit-tested with mocks, and deliberately not live-tested against a real key, same reasoning as the Anthropic path from 2026-09-13. Kept Anthropic rather than deleting it: it's already correct, costs nothing to leave in place, and the user has changed the target provider once already this week.
- 2026-09-15: User asked how to "decrease limitations" for real, not hide them, and specifically proposed translating only the final English answer for Hindi rather than building native Hindi retrieval — correct instinct, kept as-is rather than second-guessed. Built `translation.py` + `POST /api/translate`: translates an already-verified English answer on request via `llm_client` (so it automatically uses whichever provider is active), never touches retrieval/generation/verification. This is a real, disclosed narrowing of PRD §6.3's literal requirement (verify retrieval quality per language) — no Hindi retrieval ever happens, so there's nothing to verify there — chosen because translating settled, grounded text can only be mistranslated, never fabricated, unlike native Hindi generation which would need a whole second retrieval-quality gate. Live-verified: real Hindi output, correct citation preservation on one run, a garbled mixed-script fragment on another and a partially-translated citation marker in a separate direct-API test — genuine local-model translation-quality limitations, same ceiling as generation/verification, disclosed via an in-UI disclaimer rather than hidden.
- 2026-09-15: Told the user directly that a multi-agent pipeline is the wrong lever for the documented local-model limitations (WIPO confusion, false-refusal rate) and recommended against building one. This project's 3-layer defense already is the substantive form of a multi-agent pattern (a draft step and an independent verify step); adding more agent hops chains more calls onto an already-slow local model (20-40s per question) for an unverified quality gain, when the actual fix (a frontier model) is already the deployment plan. Not built; recorded as a considered-and-rejected direction, not an oversight.
  **Amendment, same day**: the user's follow-up proposal was more specific than "add agents" — a relevance check between retrieval and generation, and a coverage check on the finished answer. Those are genuinely different from Layer 1 (checks vector *distance*, not topical relevance) and Layer 2 (checks a *claim* against its *passage*, never the assembled *answer* against the *question*). Commissioned an Opus-run architecture plan (isolated worktree, read the actual code) rather than freehand it; its verdict was to build both, but as non-blocking narrowing/disclosure stages, never as refusal gates — see the two entries below. This doesn't contradict the entry above: the objection was to chaining agent hops for their own sake, not to closing a real, distinct verification gap when one exists.
- 2026-09-15: Built Stages 3 and 5 (`src/relevance.py`, `src/coverage.py`) per the Opus plan. Both single-call, not majority-vote, on the rule the plan articulated and that's now the project's standard: majority-vote where a call can delete content (Layer 2), single-call where it cannot (these two, which only narrow/annotate). Both fail open on any error — a relevance failure keeps every passage, a coverage failure just means no verdict, never a negative one. Neither is labeled "L4"/"L5" in the UI — those tags map to the PRD's own 3-layer defense, and these two stages are additions beyond it, not part of it; minting new layer numbers would overclaim PRD alignment. Terminology to use externally: "a five-stage, fixed-sequence verification pipeline," not "multi-agent" (no planner, no dynamic routing, no tool use — precise language that survives a judge's probing, per the plan's own reasoning).
- 2026-09-15: Found a real, more serious bug while live-testing the new relevance stage: its very first live run failed to parse (fewer verdicts than passages). Root cause, confirmed directly: one retrieved chunk (`wipo_documenting_tk_toolkit::sec-2012-sub-1`) was **79,710 characters** — a PDF line-wrap had put "2012." (from "...published in November 2012. Mr. Ruiz...") at the start of a line, which `SECTION_RE` read as a real section header "2012.", so the chunker treated the rest of that 40-page document as one section's body. This chunk could silently have hurt *generation* too — an oversized chunk overflowing the local model's context produces a plausible-looking but partial answer with no error at all, unlike the relevance stage's strict per-index check, which is what actually surfaced this. Fixed at the source (`chunking.py`'s `_PLAUSIBLE_YEAR_RE`): a 4-digit "section number" in the 1900–2099 range is rejected as a section match, since no document in this corpus has anywhere near that many real sections. Result: `wipo_documenting_tk_toolkit` went from 10 chunks (one of them 79,710 chars) to 56 chunks (largest now 17,978 chars — a WIPO publication's back-matter documentation-template/checklist appendix, a real but much smaller and different edge case, disclosed rather than chased further today). All other 6 documents: byte-for-byte identical chunk counts, confirmed by re-running Phase 1. Also added `PASSAGE_PREVIEW_CHARS=400` truncation specifically for the relevance prompt (judging topical relevance doesn't need a passage's full text) as defense-in-depth against the next oversized chunk nobody's found yet.
- 2026-09-15: **Ran the A/B measurement the plan required before trusting the relevance filter, and it failed its own pass criterion.** Full 16-question eval set, index rebuilt with the chunking fix above, both runs back to back on the same corpus:

  | Metric | With filter | Without filter |
  |---|---|---|
  | Citation accuracy | 4/11 | 4/11 |
  | False-refusal rate | **6/11** | **4/11** |
  | Correct-refusal rate | 5/5 | 5/5 |
  | False-answer rate | 0/5 | 0/5 |
  | Avg. time/question | 21.7s | 16.5s |

  The plan's own pre-committed rule: "if false refusals rise, the filter is dropping load-bearing passages — raise min_keep, loosen the prompt, or cut the stage." They rose (4 → 6, two concrete regressions: the biological-material-disclosure question and the wrong-geographical-origin question both answered correctly without the filter and were false-refused with it), citation accuracy didn't improve at all, and it cost ~5s/question. Correct-refusal and false-answer rates were unaffected both ways — the core safety property held regardless — but that's not the criterion that mattered here. Cut it: `relevance_filter` now defaults to `False` in both `answer_query_streaming` and `answer_query`. The code, tests, and UI wiring all stay — `relevance_filter=True` still works and `run_phase6.py --no-relevance-filter` still A/Bs it — this is "not enabled by default," not "deleted." Retuning candidates for later, not attempted today: raise `MIN_KEEP` above 3, or loosen the relevance prompt's threshold for what counts as "off-topic." `coverage_check` is unaffected by this finding (advisory-only, can't cause a refusal, wasn't implicated) and stays on by default. Also fixed a real UI bug this default flip would otherwise have shipped: the "Match to question" step was unconditionally shown and activated from the gate-done handler, which would have spun forever with the stage disabled — it's now `hidden` by default and self-activates from its own `relevance/start` event, mirroring the existing "condense" step's pattern; "generation" now activates from its own `generation/start` event instead of being chained from whichever stage runs before it.
- 2026-09-16: `bm25_retrieval.py`'s `tokenize` upgraded from a bare lowercase `\w+` split to add a ~38-word English stopword list and a small hand-rolled suffix stemmer (no NLTK), after measuring that the bare split put the Biological Diversity Act's table-of-contents chunk as BM25's #1 hit for the penalty-provision question (11/15 query tokens were stopwords or the Act's own name) and never matched "penalty"/"Penalties" or "contravening"/"contravenes"/"contravention" to each other. Two narrow additions beyond the given suffix list were needed to actually collapse those two pairs: "ies" -> "y" (not a bare strip) so "penalties" stems to "penalty" instead of "penalt", and "tion" added alongside the existing "ation" so "contravention" stems to "contraven", matching "contravenes"/"contravening". Deliberately did not stem "not"/"without"/"shall"/"may" out of the stopword list — legal negation/modality. Measured on `run_retrieval_eval.py` (before: `corpus/eval_results/retrieval_before.json`, after: `retrieval_after_bm25.json`): BM25 rank per scored question, before -> after — Q1 (TK-patentability): >40 -> 8; Q2 (biological material sourcing): 11 -> 12; Q3 (prior-art database): 5 -> 1; Q4 (patent count): 1 -> 1; Q5 (AYUSH systems): 2 -> 1; Q6 (geographical origin disclosure): 6 -> 6; Q7 (mere discovery): 1 -> 1; Q9 (BD Act approval section): 8 -> 14; Q10 (BD Act penalty, the motivating case): >40 -> 4; Q11 (TRIPS interaction): 5 -> 9. No gold passage left the BM25 top-40 that was in it before (the pre-committed MUST-NOT). Aggregate: passage recall@8 held at 8/10, passage recall@40 improved 9/10 -> 10/10, doc recall@8 held at 11/11, all 4 unrelated controls still stopped. Kept as-is (met the pass bar); a separate cross-reference change, not built here, is still expected to close the remaining Q1/Q10 recall@8 gap per the task's own framing.
- 2026-09-16: Built a permanent Layer 2 precision battery (`src/run_verification_eval.py`, 15 hand-checked claim/passage cases, 3 seeded votes x 3 seed bases each) after diagnosing `verify_claim` rejecting TRUE claims by inventing an unstated "exclusivity" requirement (rejecting "contravening section 6 ... five years" because the passage also names sections 3/4) and demanding exact date-wording match ("as of March 2013" vs. the passage's own "as on 31st March, 2013"). Measured baseline (original prompt) first: true-accepts 15/18, false-accepts 0/27 (docs/../corpus/eval_results/verification_battery_current.json). Added one precision sentence to `VERIFICATION_PROMPT_TEMPLATE` telling the model to judge the claim exactly as written — no invented exclusivity, equivalent date/figure wording counts as stated, but every figure/date/entity/section-number/negation must still match exactly — and re-ran the same battery: true-accepts 18/18, false-accepts 0/27 (corpus/eval_results/verification_battery_precise.json). Passed both pre-committed criteria on the first attempt (no second wording needed): zero false-accepts at any seed base, and true-accepts strictly improved. `verify_claim` now uses `VERIFICATION_PROMPT_TEMPLATE_PRECISE`; the original prompt is kept as `VERIFICATION_PROMPT_TEMPLATE_ORIGINAL` for the battery's baseline and as a rollback path. 140 tests pass (`tests/test_verification_eval.py` new, both with real llm_client.complete mocked out).
- 2026-09-16: `chunking.py`'s dash-less section-title fallback (recovers real headings for the Biological Diversity Act, 2002, whose sections print "55. Penalties." with no em/en-dash — every one of its ~86 sections was falling through to the generic "Paragraph N") is guarded by more than "short and ends in a period," because that alone false-positived on two numbered-paragraph guideline sections whose opening sentence happens to be short, capitalized, and period-terminated (e.g. "Indian law has adequate provisions for the protection of TK and Biological Resources."). Added a word-count cap (16, the longest confirmed real BDA title) and a finite-verb exclusion (real Act titles are noun phrases/infinitives — "...not to be made without approval," "...to be laid before Parliament" — and never use a conjugated verb like "has"/"is"/"recognizes" the way explanatory prose does; "to have"/"to had" deliberately excluded from the banned list since one real title legitimately uses "to have effect"). Result verified by diffing all 491 chunks before/after: chunk_ids and `text` are byte-identical everywhere, and all 80 recovered headings are in `biological_diversity_act_2002` — zero heading changes anywhere else, not just "essentially none."
- 2026-09-16: Fixed a generation-side spelling-fidelity bug, not a verification-side one: generation wrote "Homeopathy" when the source passage said "Homoeopathy", and Layer 2 correctly rejected the claim over the mismatch — the claim itself was right, only its wording drifted from the passage. Fixed by adding one sentence to `GENERATION_PROMPT_TEMPLATE` telling generation to copy the passage's own spelling/wording verbatim for names, terms, section numbers, figures, and dates, rather than loosening Layer 2's exact-match strictness for these fields — Layer 2's whole job is catching claims that don't match their cited passage, and a verifier that starts tolerating near-misses on entities/figures/dates is the wrong place to absorb a problem generation caused by paraphrasing in the first place.
- 2026-09-16: Live eval found Layer 2's precise prompt rejecting a TRUE claim over a spelling variant: claim "AYUSH covers Ayurveda, Siddha, Unani, Sowa-Rigpa, Homeopathy, and Yoga & Naturopathy" against `ipo_ayush_examination_guidelines_2025::preamble` (which spells it "Homoeopathy"), rejected with reasoning calling the British/American spelling difference "a slight discrepancy" — the same medical system, not a different fact. Attempted fix: extended the precise prompt's existing date/figure/entity equivalence sentence to also cover spelling variants of the same term. Added two permanent battery cases to `src/run_verification_eval.py` (now 7 true / 10 false, 17 cases x 3 seed bases): T7 (this exact Homeopathy/Homoeopathy claim, expected true) and F10 (the adversarial pair against the same passage with "Chiropractic" substituted in — a genuinely different, unsupported system — expected false, to confirm the spelling carve-out doesn't also loosen entity-substitution detection). Battery result on the candidate prompt: true-accepts 21/21 (T1-T7 all accepted at all 3 seed bases, including the new T7), false-accepts 1/30 — F10 correctly rejected 3/3, but **F8** (the existing adversarial date-substitution case, claim says "March 2015" against a passage saying "March 2013") regressed to accepted at one seed base (1101/1202/1303), where it had previously always been rejected (0/27 false-accepts recorded for the shipping precise prompt on the prior 15-case battery). Per the pre-committed criterion — any regression of an original false case to accepted, at any seed base, is a hard reject regardless of true-accept gains — this was **rejected**, not kept: `VERIFICATION_PROMPT_TEMPLATE_PRECISE` was reverted to its prior text, byte-identical (verified by diff against the pre-edit string). The two new battery cases (T7, F10) were kept permanently in `run_verification_eval.py` since they're valid regardless of this outcome. Root defect is still open: a spelling-variant claim from `generate.py` can still be false-refused by Layer 2 today; the 2026-09-16 generation-side fix (above, "copy the passage's own spelling verbatim") is the mitigation actually shipping, and this session's finding is evidence that fix needs to hold reliably, since loosening Layer 2 itself isn't safe to do casually — it reopened a different false-accept, not a free lunch. 186 tests pass (`tests/test_verification.py`/`test_verification_eval.py` updated: totals test reflects the now-permanent 7/10-case battery, no new prompt-content assertions since the prompt itself didn't change).
- 2026-09-16: Built `src/enrichment.py` — a same-document cross-reference enrichment (`build_retrieval_text`) applied only to the text embedded/tokenized for scoring, never to what a hit returns (Chroma `documents` in `indexing.py` and BM25 hit `text` in `bm25_retrieval.py` both stay the chunk's true text). Fixes the Biological Diversity Act penalty question directly: sec-55 ("Whoever contravenes ... section 3 or section 4 or section 6") never itself says "access"/"approval" - that's in sections 3/4/6, referenced only by number - so no amount of sec-55-local tuning could close the gap. Excludes cross-statute references (e.g. sec-3's own "clause (30) of section 2 of the Income-tax Act, 1961" must never pull in the BDA's own section 2 heading) by requiring the reference not be immediately followed by "of the <Name> Act"; also skips self-references and references with no recovered heading, caps at 5 references / ~420 chars (measured: the real sec-55 trailer for all of sections 3, 4 and 6 is 401 chars - 420 keeps that realistic case instead of arbitrarily dropping the third reference over one character). Measured on `run_retrieval_eval.py` (before: `corpus/eval_results/retrieval_after_bm25.json`, after: `retrieval_after_enrichment.json`): the penalty question's sec-55 rank went vector >40 -> 3, bm25 4 -> 7, hybrid 20 -> 4, **final (into generation) absent -> 3** - the GOAL, not just reaching top-40. Every other question's vector/bm25/hybrid/final rank and best_distance is byte-identical before/after (enrichment only ever touched chunks with a qualifying same-document reference, and none of the other 10 answerable questions' gold chunks had one). All pre-committed MUST-NOTs held: no gold passage in the previous final top-8 fell out, doc recall@8 stayed 11/11, passage recall@40 stayed 10/10, passage recall@8 improved 8/10 -> 9/10, all 4 unrelated controls stayed stopped, and gate_passed was unchanged for every answerable, unanswerable and control question. Kept as shipped (no revert needed). 140 tests pass (`tests/test_enrichment.py` new, including a leakage test that runs the real `indexing.build_index` Chroma path and `BM25Index` with a fixture corpus and asserts every returned hit's text is byte-identical to the source chunk with no "[Cross-references:" anywhere in it).
- 2026-09-16: Second defect found on the same held-out question set (still `src/enrichment.py`, additive to the cross-reference trailer above): short chunks that never state their own document's identity lose retrieval to longer chunks that happen to repeat the question's topic words - e.g. the WIPO GRATK Treaty's Article 17 ("This Treaty shall enter into force three months after 15 eligible parties ... have deposited their instruments of ratification or accession.") ranked vector >40 for "When does the WIPO treaty on genetic resources and associated traditional knowledge come into force?", and the Patents Act's Sec-3(i)/3(j) clauses similarly ranked >40 for "Can ... be patented in India?" / "Are medicinal plants ... patentable in India?" - none of these chunks' own text contains "WIPO", "treaty", "patent", or "India". Fixed by `build_context_header`: prepends ONE strictly-factual header line - `[<document short_name> — <section label>: <heading>]` - built only from `authority.py`'s existing `short_name` field plus the chunk's own `section_number`/`heading` metadata (no invented words describing what a section is "about"). Section label wording is itself metadata-derived, not invented: treaty `section_number` already stores "Article N" verbatim (used as-is); every other doc uses `authority.py`'s existing `authority_level` to pick "§N" for an Act vs. "paragraph N" for a Guideline/Informational numbered item, matching real legal-document terminology. Generic placeholder headings ("Paragraph N", "(unstructured)", "Preamble", "Front matter / table of contents") are omitted from the header the same way they already are from cross-reference trailers. Capped at 200 chars (measured real headers: 60-90 chars). Same safety property as the trailer: scored only, `CONTEXT_HEADER_ENABLED` module flag defaults `True` and makes `build_retrieval_text` byte-identical to the pre-header function when set `False`, for the lead's A/B measurement. 185 tests pass (`tests/test_enrichment.py` extended: header-format tests against real `authority.py` doc_ids, a header+trailer-ordering test, a `CONTEXT_HEADER_ENABLED=False` byte-identical test, and both leakage tests upgraded to use a real authority.py doc_id — `patents_act_1970` — so they actually exercise header leakage, not just trailer leakage, asserting no returned hit ever contains the header's own "[Patents Act, 1970" marker).
- 2026-09-16: Fixed a second, distinct generation-side fabrication bug on the same "wrongly discloses geographical origin" question: generation drafted a claim citing passage `patents_act_1970::sec-25-sub-1` (Section 25 opposition grounds) but attributed it to "Section 10(4)(a) & (b) of the Patents Act" — a section number never mentioned in that passage at all. Layer 2 correctly rejected it (verifier's own words: the passage supports Section 25(1)/25(2) opposition, "it does not explicitly state that such actions can be objected to or result in refusal under Section 10(4)(a) & (b)"). This is generation fabricating a section-number attribution not present in its own cited passage — a real defect, not a false rejection — so the fix is generation-side, not a Layer 2 loosening: added one sentence to `GENERATION_PROMPT_TEMPLATE`, next to the spelling-fidelity sentence above, requiring any section/sub-section/clause number a claim cites as legal basis to be the one that actually appears in the cited passage, never one recalled from general knowledge or a different passage. Spot-checked (not a full eval) against the real corpus/Ollama at top_k=8, 3 seeds (42/1042/2042, temperature 0.0 so identical across seeds as expected): the model still misattributes — it now writes the same "Section 10(4)(a) & (b)" claim but cites source 6 (`ipo_tk_biological_material_guidelines_2012::sec-5`, which discusses Section 15/25(1)/25(2) only), when the corpus passage that actually contains "section 10 (4) (a) & (b)" verbatim is a different one in the same top-8 (`ipo_tk_biological_material_guidelines_2012::sec-17`, passage 5). So the prompt addition did not resolve this case in the spot-check; the mismatch persists in a new form (right number, wrong passage index this time instead of an invented number). Flagging for the lead rather than iterating further on the prompt — this may need a stronger structural fix (e.g. instructing the model to quote the exact source sentence, or a post-hoc check that a cited section number appears in the numbered passage claimed).
- 2026-09-17: Root-caused a silent Ollama prompt-truncation bug that was very likely the single largest cause of every "local-model quality" failure reported to this point: `llm_client.py` called Ollama with no `options.num_ctx`, so Ollama silently defaulted to 2048 tokens and front-truncated any longer prompt (kept the end, discarded the start). Measured directly: a 42,467-char prompt returned `prompt_eval_count: 2050`; a marker placed at the prompt's START was invisible to the model, one at the END was visible. Since the generation prompt's structure is [instructions, question, passages], front-truncation destroys the instructions and the question first — before the model ever sees what's being asked. Fixed: `OLLAMA_NUM_CTX` (`llm_client.py`) now defaults to 8192, env-overridable. Direct before/after proof: eval question 8 ("What are the three phases the WIPO toolkit divides TK documentation into?") was a false refusal before this fix and became a correct, verified answer matching the expected text exactly ("before documentation, during documentation, and after documentation") immediately after, no other change made.
- 2026-09-17: Made LLM sampling reproducible — explicit `temperature`/`seed` params threaded through `llm_client.complete()` to all three providers (Anthropic has no seed param; documented as unsupported there, not silently ignored). Generation: `temperature=0.0`, fixed `GENERATION_SEED`. Layer 2 verification: `temperature=0.3` with 3 distinct fixed seeds (`VERIFICATION_SEEDS`) per vote, deliberately NOT 0.0 — at temperature 0 all 3 "votes" would return the identical answer, silently collapsing majority-vote into one repeated call. Documented in `verification.py`'s own comments as a deliberate design choice, not an oversight.
- 2026-09-17: Fixed a hybrid-retrieval candidate-pool bug: `generate.py` called `retrieve_hybrid(query, top_k=CANDIDATE_K)` (CANDIDATE_K=40) without passing `candidate_k`, so RRF fusion only ever combined each retriever's top 20 while Layer 1's own vector-only check looked 40 deep — the two halves of the pipeline disagreed about how far to look. Fixed by passing `candidate_k=CANDIDATE_K` explicitly; `retrieve_hybrid` now also clamps `candidate_k` up to at least `top_k` so this class of bug can't recur silently.
- 2026-09-17: Found and fixed a real statutory-clause retrieval gap: `biological_diversity_act_2002::sec-55` (the correct answer to the penalty question) was absent from the top 40 results of vector search, BM25, AND hybrid, all three, before today. Root cause: §55's actual text ("Whoever contravenes ... section 3 or section 4 or section 6 shall be punishable...") never contains the word "access" — that concept lives in §6, referenced only by number — while §56 ("Penalty for contravention of directions or orders...") ranked 3rd on vector search purely for containing the literal phrase "Penalty for contravention," so the system was retrieving the wrong penalty section entirely. Fixed with `src/enrichment.py`: index-time-only cross-reference enrichment appends a short trailer to a chunk's SCORED text naming the headings of same-document sections it cross-references by number, so §55's scored text now contains the vocabulary its own cross-reference implies. Cross-statute references (e.g. the Income-tax Act reference inside the BD Act's own text) are excluded via a negative-lookahead regex. Required first fixing `chunking.py` to recover real section headings for the BD Act (its "55. Penalties." heading style, unlike the Patents Act's em/en-dash style, had every one of its ~86 sections falling through to a generic "Paragraph N" placeholder) via a guarded fallback (word-count cap, finite-verb exclusion). Verified zero effect elsewhere: all 491 chunk_ids and every chunk's text stayed byte-identical; only headings on the BD Act's ~80 sections changed. **Safety property enforced by an automated leakage test, not just a comment**: the enriched text exists only inside the embedding/BM25-scoring step — never returned as a retrieved passage's text, never shown to the user, never given to generation, never verified against, cannot become part of a citation; a dedicated test builds a real index and asserts every returned hit's text is byte-identical to the source chunk with no trace of the enrichment marker. Result, measured: §55 went from absent (rank >40, all three retrievers) to **rank 3** in what generation actually receives (final top-8), with correct-refusal rate, the 4 unrelated-topic controls, and every other question's retrieval rank unchanged before/after.
- 2026-09-17: Fixed BM25 keyword matching (`bm25_retrieval.py`): the tokenizer was a bare lowercase word-split with no stemming/stopword removal. On the §55 penalty question, 11 of 15 query words were stopwords or the document's own name, and "penalty" never matched "Penalties" nor "contravening" matched "contravenes"/"contravention," so the correct chunk scored nothing on BM25 while §56 (the wrong section) matched literally. Added a ~38-word stopword list (kept legally-significant words — "not," "no," "without," "shall," "may" — since negation changes legal meaning) and a small hand-rolled suffix stemmer (no new dependency), ASCII-only so Devanagari text is untouched. Measured on `run_retrieval_eval.py`: no gold passage for any of 10 dev-set questions left the BM25 top-40; passage-level recall@40 improved 9/10 → 10/10.
- 2026-09-17: Added a contextual header to indexed text (`enrichment.py`, scored-only, same leakage-tested safety property as the cross-reference trailer above): short chunks whose own text never restates the document's subject (e.g. `wipo_gratk_treaty_2024::article-17`, 77 chars, never says "WIPO"/"treaty") lose to longer chunks that happen to repeat the question's topic words. Added a short, strictly factual header (document title + section label + real heading only, no invented words, capped ~200 chars) prepended to the scored text only. Measured, reported honestly as a partial result: real rank improvements on several questions (the §55 question's BM25/hybrid rank improved further; a TRIPS-interaction question's hybrid rank went 6 → 2), dev-set citation accuracy held or improved with zero regressions — kept for that reason. But on its own three originally-motivating held-out cases it did NOT fully close the gap: ranks improved meaningfully (one hybrid rank 40 → 17, another 20 → 13) but none crossed into the top-8 generation actually receives. Reported as partial, not solved.
- 2026-09-17: Corrected two stale eval ground-truth entries in `run_phase6.py`'s `ANSWERABLE` list: the two Biological Diversity Act questions' expected-source sets were written 2026-09-12, one day BEFORE the BD Act was added to the corpus (2026-09-13) — a list written before a document existed could not have considered it, so the system was citing the Act's own §6/§55 correctly and being scored wrong. `docs/eval_questions.md` already named these exact chunks as correct on 2026-09-13; only the code list had lagged. Fixed by adding `biological_diversity_act_2002` to both expected-source sets, chunk text quoted in a code comment as evidence; this makes the metric easier, so pre-/post-correction numbers are reported separately, never conflated. A second, similar transcription error was found and fixed in `run_retrieval_eval.py`'s own gold table: the TRIPS-interaction question's gold source was mis-transcribed as `pib_faq...::para-3` when the actual TRIPS discussion is in `::para-2` (verified by reading both chunks — para-3 is about BD Act §6 approval and never mentions TRIPS). `docs/eval_questions.md` question 12 corrected to match.
- 2026-09-17: Two generation-side prompt fixes in `GENERATION_PROMPT_TEMPLATE`: (a) copy the passage's own spelling/wording exactly for names, terms, section numbers, figures and dates (found via a real case: generation wrote "Homeopathy," the passage said "Homoeopathy," Layer 2 correctly rejected the mismatch as written — copying exact spelling avoids manufacturing a discrepancy that was never a factual one); (b) any section/sub-section number a claim states as its legal basis must be the one actually in the cited passage, never recalled from a different passage or general knowledge (found via a real case: generation cited "Section 10(4)(a) & (b)" while its actual cited passage discussed "Section 25(1)/25(2)" — a real, correct number from elsewhere in the corpus, attributed to the wrong passage; Layer 2 correctly caught and refused it). Honest note: a direct 3-seed spot-check after shipping (b) found it reduced but did NOT fully eliminate this failure mode — the model shifted from inventing a number from nowhere to occasionally still misattributing a real number between two passages in the same prompt. Documented as real, partially-mitigated, still open — not claimed fixed.
- 2026-09-17: Tried and rejected an unsafe verification change — a positive example of the project's own safety discipline, not a hidden failure. Attempted to extend Layer 2's precise prompt to accept spelling variants ("Homeopathy"/"Homoeopathy") as equivalent, testing against the permanent adversarial battery (`run_verification_eval.py`) at 3 seed bases. The change fixed its target case (all seeds now accepted the spelling-variant claim) but simultaneously caused a different, previously-always-rejected false claim (a wrong date, "March 2015" substituted for "March 2013") to be wrongly ACCEPTED at one seed base — a genuine new false-accept, the one thing this project's safety bar can never trade away. Per the pre-committed rule, rejected and reverted to the exact prior prompt, byte-identical, confirmed by diff. The underlying defect (spelling-variant claims occasionally false-refused) remains open, mitigated only by the generation-side spelling-fidelity fix above, which is not 100% reliable either.
- 2026-09-17: Built two held-out question sets, frozen before any fix they motivate was measured — `docs/heldout_questions.json` (v1, authored after all dev-set tuning, before ever being run) and `docs/heldout_questions_v2.json` (v2, authored and frozen after v1 exposed the short-chunk defect the contextual header targeted, specifically to test whether that fix generalizes to questions it was never shaped by). Both committed to git BEFORE being run through the pipeline (commit `4a7a66d`) — this is the actual, verifiable anti-overfitting evidence, not just an assertion. Every gold answer chunk was opened and read to confirm it states the expected fact; every "unanswerable" question was grep-confirmed absent from the corpus before being used.
- 2026-09-17: **Final measurement, three separate 3-run evaluations** (`run_phase6.py --runs 3 --seed-base 42`, worst case headlined per the project's own stated rule, not the best) on the fully-fixed system.
  Dev set (11 answerable / 5 unanswerable): citation accuracy min/median/max 8/9/9 (of 11), false refusals 1/1/2 (of 11), correct refusals 5/5/5 (of 5), false answers 0/0/0 (of 5). Compare pre-session baseline rescored against the same corrected ground truth: citation accuracy 5/11, false refusals 4/11, correct refusals 5/5, false answers 0/5, ~16.5s/question. Worst-case citation accuracy 5/11 → 8/11, worst-case false-refusal rate 4/11 → 2/11; both zero-hallucination numbers held exactly across every measurement. 9/11 answerable questions passed all 3 runs; the 2 that didn't are diagnosed directly in README.md's "Known limitations" (a section-number-misattribution case, and a genuine model-comprehension limit on "how does X interact with Y" synthesis framing — confirmed by isolating the right passages alone and still getting zero drafted claims across all 3 seeds).
  Held-out v1 (10/5): citation accuracy 5/6/6, false refusals 3/3/4, correct refusals 5/5/5, false answers 0/0/0. 5/10 passed all 3 runs; 4 failed all 3 (3 are the contextual-header's own not-fully-closed short-chunk cases, the 4th a new instance of the same "picks a topically-plausible but not-quite-right passage" pattern); 1 unstable (2/3).
  Held-out v2 (10/5, frozen after v1 and after the contextual-header fix, never used to tune anything): citation accuracy 6/6/6, false refusals 4/4/4, correct refusals 5/5/5, false answers 0/0/0 — perfectly stable, zero variance on any metric across all 3 runs. 6/10 passed every run; the 4 that failed every run are diagnosed directly: one genuine retrieval miss, two more instances of the TRIPS-style zero-claims synthesis limitation, and a second concrete example of Layer 2 over-literalism (the Tribal Digital Document Repository claim — generation drafted the exact correct claim word-for-word, retrieval gave it the single exactly-right passage, and Layer 2 rejected it 3/3 reasoning the passage "does not provide any information about its listing in AYUSH examination guidelines," a confused reading since the passage's presence in that document constitutes it being listed there).
  **Headline**: 96 total question-runs across 3 question sets x 3 seeded runs each — correct-refusal rate 5/5 and false-answer rate 0/5 held on every single run, no exceptions, anywhere, including on two question sets built specifically to catch overfitting. The system has never, in any measurement taken this entire session, produced a confidently-stated false answer.
  **The held-out methodology is this session's most important new discipline**, not just another fix: every prior number in this log was measured on the same dev set used to find and fix the bugs it reports, which cannot distinguish a real fix from overfitting to that set. Freezing v1 and v2 before running them (verifiable via commit `4a7a66d`, not just asserted) is what makes "generalizes" a checked claim instead of a hoped one for this project going forward.
- 2026-09-17: 186 tests pass (`pytest tests/ -q`). Grepped the whole repo for "multi-agent" (case-insensitive): all remaining occurrences are the established, correct usage — explicitly declining or contrasting against "multi-agent" as the wrong term for this pipeline (README.md, architecture.md, phases.md, decisions.md, memory.md) — zero instances of this project mislabeling itself "multi-agent."
- 2026-09-18: A live user report (screenshot: raw `ConnectionError: HTTPConnectionPool(host='localhost', port=11434)... [WinError 10061]` text rendered directly in the chat UI's error box) surfaced a gap the earlier `ProviderRateLimited` work didn't cover: Ollama being unreachable (down, wrong port, or hung past its timeout) is a different failure from a cloud provider's rate limit, but was going uncaught entirely — `requests.exceptions.ConnectionError`/`Timeout` propagated raw from `_complete_ollama` straight to the SSE layer. Added a sibling exception, `ProviderUnreachable`, kept deliberately separate from `ProviderRateLimited` rather than merged under one name or renamed to something generic — a rate limit was reached and said "slow down" (often clears in seconds, worth a short retry); "unreachable" was never reached at all (retrying doesn't help, fails immediately, single attempt). `generate.py`/`verification.py` react to both the same way (the same `provider_unavailable` SSE event, message reworded to "isn't available" so it reads honestly for either cause) — the class names stay honest about *why* even though the user-facing reaction is unified. 208 tests pass (up from 200); live-smoke-tested against the real running Ollama to confirm the working path is unaffected.
- 2026-09-20: Stopped pursuing a hosted deployment entirely rather than degrading the app to fit a free tier. Three platforms were tried and each failed on a hard resource limit, not a fixable configuration error: Hugging Face Spaces (free accounts may run exactly one Space; the slot was already occupied), Oracle Cloud Always Free, and Render (512 MB RAM ceiling against this app's ~3-4 GB floor — bge-m3's weights plus PyTorch). The alternative would have been swapping bge-m3 for a smaller embedding model, which would invalidate every retrieval number measured across the 96 evaluation runs; keeping the measurements honest was worth more than a live URL. Deployment-specific scaffolding (Spaces YAML frontmatter, docker-compose.yml, Caddyfile) removed; the Dockerfile stays, since it still builds a working local container. The README now states plainly that no hosted instance exists.
- 2026-09-20: BM25 now abstains on a query it cannot match, instead of returning `top_k` arbitrary chunks. `BM25Index.retrieve` ranked every chunk and sliced the top k without checking that any chunk matched at all, so a query sharing no token with the corpus got back the first k chunks in corpus order, each scoring 0.0. Hybrid fusion made this actively harmful rather than merely useless: RRF weights by rank, not score, so a 0.0-scoring chunk at BM25 rank 1 contributed exactly as much as a genuine top-ranked English hit. Every pure-Devanagari query hits this against an English corpus — two topically unrelated Hindi questions returned an identical chunk list, which is what exposed it. Filtering on token overlap rather than `score > 0`: BM25 IDF is exactly zero for a term appearing in half the corpus, so a score test discards real matches in a small index (`tests/test_enrichment.py`'s two-chunk index is one, and it caught this on the first attempt). Measured over ten parallel Hindi/English question pairs: Hindi expected-source-in-top-5 7/10 → 8/10, mean EN/HI top-5 overlap 2.3/5 → 3.1/5, English unchanged at 9/10 (English queries always had real matches, so the fix is a no-op there, which is the control that says the gain is real). Three regression tests added, each confirmed to fail without the fix.
- 2026-09-20: Cross-lingual retrieval is measured with parallel question pairs rather than blocked on building a Hindi corpus. `docs/eval_questions.md` had category E marked BLOCKED on the reasoning that there was "no Hindi ground truth to verify against." That premise was wrong: a Hindi question and its English twin should retrieve the same source chunks, so section A's English ground truth is also the Hindi ground truth. This measures precisely what PRD §6.3 asks — whether a Hindi speaker reaches the same sources as an English speaker — with no corpus change and no re-indexing. Native Hindi documents would test a genuinely different thing (retrieval quality *within* Hindi) and remain worth adding, but they are no longer a prerequisite for having any multilingual number at all.

